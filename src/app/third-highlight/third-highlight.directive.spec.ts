import { ThirdHighlightDirective } from './third-highlight.directive';

describe('ThirdHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ThirdHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
